PreProcessor is to deal with "include path/path/file" model.
the directive like #if, #else can not be used for simpilicity.
the preprocessor is done after the AST is built before the typebuilder is done.
